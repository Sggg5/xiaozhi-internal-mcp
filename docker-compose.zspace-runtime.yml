services:
  github-sync:
    image: public.ecr.aws/docker/library/node:22-bookworm
    restart: unless-stopped
    working_dir: /repo
    command: node /repo/docker/github-sync/sync-github.mjs
    environment:
      GIT_REPO_URL: ${GIT_REPO_URL:-https://github.com/Sggg5/xiaozhi-internal-mcp.git}
      GIT_BRANCH: ${GIT_BRANCH:-main}
      SYNC_INTERVAL_SECONDS: ${SYNC_INTERVAL_SECONDS:-3600}
    volumes:
      - .:/repo

  internal-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      MCP_TOKEN: ${MCP_TOKEN}
      PORT: "3000"
      XIAOZHI_DATA_DIR: /app/data
    volumes:
      - ./data:/app/data:ro
    healthcheck:
      test:
        - CMD
        - node
        - -e
        - fetch('http://127.0.0.1:3000/health').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

  xiaozhi-client:
    image: public.ecr.aws/docker/library/node:22-bookworm-slim
    restart: unless-stopped
    depends_on:
      internal-mcp:
        condition: service_healthy
    working_dir: /opt/xiaozhi
    command:
      - sh
      - -c
      - npm install -g xiaozhi-client@2.3.0 --ignore-scripts && node /opt/xiaozhi/entrypoint.mjs
    environment:
      XIAOZHI_ENDPOINT: ${XIAOZHI_ENDPOINT}
      MCP_TOKEN: ${MCP_TOKEN}
      MCP_URL: http://internal-mcp:3000/mcp
      XIAOZHI_CONFIG_DIR: /data
    volumes:
      - ./docker/xiaozhi-client/entrypoint.mjs:/opt/xiaozhi/entrypoint.mjs:ro
      - xiaozhi-data:/data
    ports:
      - "${XIAOZHI_WEB_PORT:-9999}:9999"

volumes:
  xiaozhi-data:
