# 极空间 Docker 部署

这套配置会在极空间上同时运行：

- `github-sync`：定时从 GitHub 拉取最新项目，并重新生成 `data/*.json`
- `internal-mcp`：尺寸查询、重量计算、报价和知识库工具
- `xiaozhi-client`：连接小智云端，并把上述工具注册给小智

## 1. 上传目录

将整个项目目录上传到极空间，例如：

```text
/docker/xiaozhi-mcp
```

不要上传本机的 `node_modules`、`dist` 和日志文件。

## 2. 创建密钥配置

在项目目录中复制：

```text
.env.zspace.example -> .env
```

编辑 `.env`：

```dotenv
XIAOZHI_ENDPOINT=wss://api.xiaozhi.me/mcp/?token=你的小智接入Token
MCP_TOKEN=独立的高强度随机密钥
XIAOZHI_WEB_PORT=9999

GIT_REPO_URL=https://github.com/Sggg5/xiaozhi-internal-mcp.git
GIT_BRANCH=main
SYNC_INTERVAL_SECONDS=3600
```

`XIAOZHI_ENDPOINT` 和 `MCP_TOKEN` 是两个不同的密钥。

## 3. 启动

如果用极空间 Docker Compose 项目，compose 内容使用 `docker-compose.zspace-runtime.yml`。

如果在命令行启动：

```bash
docker compose --env-file .env -f docker-compose.zspace-runtime.yml up -d --build
```

查看日志：

```bash
docker compose --env-file .env -f docker-compose.zspace-runtime.yml logs -f
```

看到 `MCP WebSocket 连接已建立` 和 `加载了 8 个工具` 即表示成功。

## 4. 数据同步

`github-sync` 默认每 3600 秒执行一次：

```bash
git fetch origin main
git reset --hard origin/main
npm run data:build
```

`internal-mcp` 挂载项目目录下的 `data/`，并会在下一次工具请求时自动检查数据文件是否变化。只改表格或知识库文章时，不需要手动重启容器。

开启同步后，GitHub 是主数据源。不要直接在极空间里修改 `data-source/` 或 `knowledge/`，否则下次同步会被 GitHub 覆盖。

## 5. 管理页面

浏览器访问：

```text
http://极空间局域网IP:9999
```

无需将 `9999` 端口开放到公网。

## 6. 更新和停止

重新构建并启动：

```bash
docker compose --env-file .env -f docker-compose.zspace-runtime.yml up -d --build
```

停止：

```bash
docker compose --env-file .env -f docker-compose.zspace-runtime.yml down
```
