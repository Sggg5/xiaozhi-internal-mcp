# MCP 数据维护

日常维护优先修改 `data-source/` 和 `knowledge/`，不要直接手写 `data/*.json`。

## 目录说明

```text
data-source/
  pipe-sizes.csv              管材规格
  fitting-sizes.csv           管件规格
  material-prices.csv         材料单价
  surface-prices.csv          表面处理费用
  customer-price-factors.csv  客户报价系数
  quote-settings.csv          报价通用参数

knowledge/
  *.md                        内部知识库文章，一篇文章一个文件

data/
  *.json                      自动生成，MCP 实际读取
```

CSV 文件可以用 Excel / WPS 直接打开编辑。多值字段用 `|` 分隔，例如：

```text
0.8|1.0|1.2
304|316L
```

## 推荐维护流程

在家里或公司都按这个流程走：

1. 修改 `data-source/*.csv` 或 `knowledge/*.md`
2. 在项目目录运行：

```powershell
npm run data:build
npm test
```

3. 提交并推送到 GitHub：

```powershell
git add data data-source knowledge scripts package.json 数据维护.md 极空间部署.md
git commit -m "Update MCP data"
git push
```

4. 极空间里的 `github-sync` 容器会定时拉取 GitHub，并重新生成 `data/*.json`。

默认每 3600 秒同步一次。同步完成后，`internal-mcp` 会在下一次工具请求时自动读取新数据，不需要手动重启容器。

## 极空间同步规则

开启定时同步后，GitHub 是主数据源。

不要直接在极空间的 `W:\xiaozhi-mcp\data-source` 或 `W:\xiaozhi-mcp\knowledge` 里改数据，因为 `github-sync` 会执行：

```bash
git fetch origin main
git reset --hard origin/main
npm run data:build
```

这会用 GitHub 上的内容覆盖极空间本地的已跟踪文件。`.env` 这类未提交的密钥文件不会被清理。

如果想在调试时更快同步，可以在极空间项目的 `.env` 里临时修改：

```dotenv
SYNC_INTERVAL_SECONDS=600
```

## 知识库文章格式

每篇文章放在 `knowledge/`，文件名随意，建议用英文或拼音。格式：

```markdown
---
id: article-id
title: 文章标题
category: 分类
tags: [标签1, 标签2]
summary: 一句话摘要
updatedAt: 2026-06-15
---

这里写正文。
```

`id` 必须唯一，后续 `get_blog_article` 会用它读取文章。
